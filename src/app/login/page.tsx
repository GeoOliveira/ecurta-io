import{redirect}from"next/navigation";import{getAdminSession}from"@/lib/auth";import{loginAction}from"./actions";export default async function LoginPage({searchParams}:{searchParams:Promise<{erro?:string}>}){if(await getAdminSession())redirect("/admin");const{erro}=await searchParams;return <main className="container state"><div className="eyebrow">Área restrita</div><h1>Acesso administrativo</h1><p className="lead">Entre com o usuário criado no Supabase Auth.</p><form className="panel auth-form" action={loginAction}><label htmlFor="email">E-mail</label><input id="email" name="email" type="email" autoComplete="email" required/><label htmlFor="password">Senha</label><input id="password" name="password" type="password" autoComplete="current-password" minLength={8} required/>{erro&&<p className="form-error" role="alert">{erro}</p>}<button className="button" type="submit">Entrar</button></form></main>}
