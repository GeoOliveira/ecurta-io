import{NextRequest,NextResponse}from"next/server";import{resolveShortLink}from"@/lib/short-links/resolve";export const runtime="nodejs";export async function GET(request:NextRequest,{params}:{params:Promise<{slug:string}>}){const r=await resolveShortLink((await params).slug);if(r.kind==="active")return NextResponse.redirect(r.destinationUrl,307);const path=r.kind==="not-found"?"/link-nao-encontrado":r.kind==="expired"?"/link-expirado":r.kind==="blocked"?"/link-bloqueado":"/erro";return NextResponse.redirect(new URL(path,request.url),307)}
