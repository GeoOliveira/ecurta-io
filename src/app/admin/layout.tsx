import Link from"next/link";import{redirect}from"next/navigation";import{getAdminSession}from"@/lib/auth";import{logoutAction}from"@/app/login/actions";export default async function AdminLayout({children}:{children:React.ReactNode}){const session=await getAdminSession();if(!session)redirect("/login");return <><header className="admin-nav"><div className="container admin-nav-inner"><Link className="brand" href="/admin">encurta<span>.io</span></Link><nav><Link href="/admin/links">Links</Link><Link href="/admin/configuracoes">Configurações</Link><span>{session.email} · {session.role}</span><form action={logoutAction}><button className="link-button" type="submit">Sair</button></form></nav></div></header>{children}</>}
