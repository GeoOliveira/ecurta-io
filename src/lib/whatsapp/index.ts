import {z} from "zod";import {BRAZILIAN_AREA_CODES}from"./area-codes";export type WhatsAppDestinationResult={normalizedPhone:string;formattedPhone:string;destinationUrl:string};
export function normalizeBrazilianPhone(input:string){let d=input.replace(/\D/g,"");if(d.length===11)d="55"+d;if(!d.startsWith("55")||d.length!==13)throw new Error("Telefone brasileiro inválido");return d}
export function validateBrazilianPhone(input:string){try{const p=normalizeBrazilianPhone(input);return BRAZILIAN_AREA_CODES.has(Number(p.slice(2,4)))&&p[4]==="9"}catch{return false}}
export const whatsappInputSchema=z.object({phone:z.string().min(1).refine(validateBrazilianPhone,"Informe um celular brasileiro válido"),message:z.string().max(1000).optional()});
export function generateWhatsAppDestination(input:{phone:string;message?:string}):WhatsAppDestinationResult{const p=whatsappInputSchema.parse(input);const normalizedPhone=normalizeBrazilianPhone(p.phone);const url=new URL(`https://wa.me/${normalizedPhone}`);if(p.message?.trim())url.searchParams.set("text",p.message.trim());return{normalizedPhone,formattedPhone:`+${normalizedPhone.slice(0,2)} (${normalizedPhone.slice(2,4)}) ${normalizedPhone.slice(4,9)}-${normalizedPhone.slice(9)}`,destinationUrl:url.toString()}}
