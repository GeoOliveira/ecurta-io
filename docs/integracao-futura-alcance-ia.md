# Integração futura com Alcance IA

Futuramente a Alcance IA poderá enviar `phone`, `message`, `expiresAt` e `source: alcance_ia` por API interna autenticada ou RPC. A resposta deverá conter `id`, `slug`, `shortUrl`, `destinationUrl` e `expiresAt`. Nenhum endpoint público foi criado.
