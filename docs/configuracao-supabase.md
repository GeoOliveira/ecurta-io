# Supabase

Crie um projeto Supabase, configure as variáveis privadas e aplique a migration local após revisão. Crie o primeiro usuário no Auth e insira seu perfil como `super_admin`. RLS permanece habilitada; a RPC pública retorna apenas a resolução necessária.
